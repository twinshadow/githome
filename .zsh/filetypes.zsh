##
# Filetype Magic!
##

# Hypertext
alias -s htm=$BROWSER
alias -s html=$BROWSER

# Images
alias -s gif=$VIEWER
alias -s jpg=$VIEWER
alias -s jpeg=$VIEWER
alias -s png=$VIEWER

# Docs
alias -s abw=abiword
alias -s chm=chmsee
alias -s doc=$WORDPROC
alias -s pdf=epdfview
alias -s rtf=$WORDPROC

# Archives
alias -s 7z=p7unzip
alias -s bz2=bunzip
alias -s gz=gunzip
alias -s rar=unrar x
alias -s xz=unxz
alias -s Z=unzip
alias -s zip=unzip
alias -s tbz2=tar -xjvf
alias -s tgz=tar xzvf
alias -s txz=tar -xJvf

